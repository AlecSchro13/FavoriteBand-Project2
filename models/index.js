const User = require('./User');
const Artist = require('./artist')
module.exports = { User, Artist };
// const app = express()

// app.get('/', (req, res) => {
//   res.send('Welcome')
// })
// console.log(User)
// app.listen(3000)